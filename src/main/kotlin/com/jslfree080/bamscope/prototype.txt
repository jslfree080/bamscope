<RunBAMScope.kt>
package com.jslfree080.bamscope

import com.jslfree080.bamscope.bam.BAMFile
import com.jslfree080.bamscope.bam.Read
import com.jslfree080.bamscope.bam.Plot
import com.jslfree080.bamscope.bam.Position
import com.jslfree080.utils.PlotUtils
import com.jslfree080.utils.NumpyUtils
import com.jslfree080.cli.BAMScopeCommand
import picocli.CommandLine

fun main(args: Array<String>) {
    val bamFile = BAMFile("input.bam")
    val reads = bamFile.getReads()
    val plot = Plot(reads)
    val positions = Position(reads)
    val plotUtils = PlotUtils(plot)
    val numpyUtils = NumpyUtils(plot)

    // call the appropriate function from CommandLine.kt to start the CLI
    CommandLine(BAMScopeCommand()).execute(*args)
}